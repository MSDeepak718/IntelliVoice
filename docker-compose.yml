# ============================================================
# IntelliVoice — Docker Compose (minimal)
# ============================================================
# No external services required. All models run locally.
# This file is kept as a placeholder for future services
# (e.g., monitoring, metrics).
#
# The application runs directly via:
#   uvicorn backend.main:app --host 0.0.0.0 --port 8000
# ============================================================
